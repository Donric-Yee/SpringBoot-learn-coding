spring:
  datasource:
    username: root
    password: Mumu9568
    url: jdbc:mysql://localhost:3306/mybatis?SSL=true&useUnicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver
#     整合mybatis
mybatis:
  type-aliases-package: com.donric.pojo  # resultType的别名
  mapper-locations: classpath:mybatis/mapper/*.xml #映射文件
